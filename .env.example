# Maia local configuration
# Generate a 64-character hex key: openssl rand -hex 32
CREDENTIAL_MASTER_KEY=

# Docker sandbox container name (must match docker-compose.yml)
SANDBOX_CONTAINER_NAME=maia-sandbox
